rootProject.name = 'checkmate-odi'

 if (file('../gradle-analytics').exists()) {
   includeBuild('../gradle-analytics') {
     dependencySubstitution {
       substitute module('gradle.plugin.com.redpillanalytics:gradle-analytics') with project(':')
     }
   }
 }
